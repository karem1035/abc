# Server-side backend API base (no trailing slash)
API_URL=http://localhost:3000/v1
# Public site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Long random secret for POST /api/revalidate (Authorization: Bearer <secret>).
REVALIDATE_SECRET=
